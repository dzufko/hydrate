{
    "name": "hydrate/hydrate",
    "description": "HTML template hydration and PDF generation with versioning",
    "type": "project",
    "require": {
        "php": "^8.1",
        "dompdf/dompdf": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "Hydrate\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}
